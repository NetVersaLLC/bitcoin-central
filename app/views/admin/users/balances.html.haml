%h2= "Balances for #{@user.name} (#{@user.email})"

.widget-container
  %table.table.table-striped
    - @balances.each do |c, b|
      %tr{ :class => cycle('even', 'odd') }
        %th= c
        %td.amount= b
